{
  "name": "ssh-server-action-action",
  "version": "1.0.0",
  "description": "Cross-platform SSH server GitHub Action",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build src/index.js -o dist --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  },
  "keywords": [
    "github-action",
    "ssh",
    "server",
    "cross-platform",
    "windows",
    "linux",
    "macos"
  ],
  "author": "GitHub Actions",
  "license": "MIT"
}